# Dependências das fases avançadas — instalar apenas nos ambientes apropriados.
#
# Fase 5a (fine-tuning Gemini 2.5 Flash via Vertex AI):
google-cloud-aiplatform>=1.40.0
google-cloud-storage>=2.16.0
google-genai>=1.0.0
#
# Fase 5b (fine-tuning Qwen 2.5 14B via LoRA/Unsloth — executar em RunPod):
# unsloth[...]  # instalar no ambiente GPU conforme a combinação real de torch/cuda
#               # ex.: no smoke test em H100 com torch 2.9.1 + cu130 usamos
#               # `unsloth[cu130-torch291]`
datasets>=2.19.0
trl>=0.9.0
peft>=0.11.0
#
# Fases 6 e 7 (baseline + avaliação no Colab/ambiente dedicado):
rouge-score>=0.1.2
bert-score>=0.3.13
transformers>=4.40.0
accelerate>=0.30.0
# torch  # instalar conforme o ambiente CPU/CUDA usado para o Qwen
# deepseek-sdk  # instalar apenas no ambiente que executará o juiz automático

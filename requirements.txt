# Dependências do pipeline — Dissertação Ementas TR
#
# Fases 1–4 (ingestão, higienização, anonimização, estatísticas):
pandas>=2.2.0
numpy>=1.26.0
#
# Fase 5a (fine-tuning Gemini 2.5 Flash via Vertex AI):
google-cloud-aiplatform>=1.40.0
#
# Fase 5b (fine-tuning Qwen 2.5 14B via LoRA/Unsloth — executar em RunPod):
# unsloth[cu124-torch260]   # instalar no ambiente RunPod, não localmente
#
# Fases 6 e 7 (baseline + avaliação no Colab — ver requirements_colab.txt):
# rouge-score, bert-score, transformers, deepseek-sdk  # instalar no Colab
